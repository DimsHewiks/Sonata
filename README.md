# 🎼 Sonata — минималистичный PHP-фреймворк
Лёгкий, гибкий и расширяемый фреймворк для быстрой разработки API с поддержкой современных практик: атрибуты, DI-контейнер, автоматическая маршрутизация.

## 📦 Пакеты
- `sonata/framework` — ядро фреймворка
- `sonata/jwt-auth` — модуль JWT-авторизации
- `sonata/docs` — модуль генерации OpenAPI

# 🚀 Быстрый старт
Требования
- PHP 8.3+
- Composer
- MariaDB / MySQL (опционально)
- docker (всё обернуто в контейнер)
- Установка 
bash:
  
        git clone https://github.com/DimsHewiks/Sonata.git
        cd sonata
        composer install
        cp .env.example .env

- для запуска

        docker-compose build --no-cache
        docker-compose up -d

## 🔐 JWT авторизация (опционально)
После установки модуля JWT выполните миграции:

```bash
php bin/console jwt:install
```


# 🧩 Основные возможности
- Быстрое создание роутов на базе внутренних атрибутов

# Структура проекта 

## Документация для frontend
- `docs/frontend-api.md` — актуальный контракт по auth и feed API.
